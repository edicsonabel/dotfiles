# kscreenlocker_greet no encuentra los modulos QML de Caelestia (Caelestia.Services,
# M3Shapes), instalados en ~/.local/lib/qt6/qml por el port a KDE: solo busca en las
# rutas del sistema. Sin esto cae al lockscreen de reserva de Plasma —el fondo azul—
# con "module not installed" en el journal.
#
# Quickshell los encuentra por su cuenta; esto es solo para kscreenlocker.
QML2_IMPORT_PATH=/home/edicsonabel/.local/lib/qt6/qml
